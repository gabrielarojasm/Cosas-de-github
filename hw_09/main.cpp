#include <iostream>
#include "funciones.h"

int main(void){ 
    {printf("\nThe value of y at x is : %f", 
             rungeKutta(0,8.5, x, 0.1, dydt);
        return 0;}
    


    {printf("\nThe value of y at x is : %f", 
             rungeKutta(0,8.5, x, 0.1, dvdt);
        return 0;
     } 
    
}